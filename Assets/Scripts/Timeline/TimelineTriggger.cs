using UnityEngine;

public class TimelineTriggger : MonoBehaviour
{
    public int id;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //TimeLineManager.instance.StartTimeline(id);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
